// WARN: This file is auto generated by cmp_lapp_xtask
{
    if aargs.flag_present("abstract-unix-socket") {
        conf.opt_abstract_unix_socket = aargs.get_string("abstract-unix-socket");
    }
    if aargs.flag_present("alt-svc") {
        conf.opt_alt_svc = aargs.get_string("alt-svc");
    }
    if aargs.flag_present("anyauth") {
        conf.flg_anyauth = aargs.get_bool("anyauth");
    }
    if aargs.flag_present("append") {
        conf.flg_append = aargs.get_bool("append");
    }
    if aargs.flag_present("basic") {
        conf.flg_basic = aargs.get_bool("basic");
    }
    if aargs.flag_present("cacert") {
        conf.opt_cacert = aargs.get_string("cacert");
    }
    if aargs.flag_present("capath") {
        conf.opt_capath = aargs.get_string("capath");
    }
    if aargs.flag_present("cert") {
        conf.opt_cert = aargs.get_string("cert");
    }
    if aargs.flag_present("cert-status") {
        conf.flg_cert_status = aargs.get_bool("cert-status");
    }
    if aargs.flag_present("cert-type") {
        conf.opt_cert_type = aargs.get_string("cert-type");
    }
    if aargs.flag_present("ciphers") {
        conf.opt_ciphers = aargs.get_string("ciphers");
    }
    if aargs.flag_present("compressed") {
        conf.flg_compressed = aargs.get_bool("compressed");
    }
    if aargs.flag_present("compressed-ssh") {
        conf.flg_compressed_ssh = aargs.get_bool("compressed-ssh");
    }
    if aargs.flag_present("config") {
        conf.opt_config = aargs.get_string("config");
    }
    if aargs.flag_present("connect-timeout") {
        conf.opt_connect_timeout = aargs.get_integer("connect-timeout") as u32;
    }
    if aargs.flag_present("connect-to") {
        conf.opt_connect_to = aargs.get_string("connect-to");
    }
    if aargs.flag_present("continue-at") {
        conf.opt_continue_at = aargs.get_integer("continue-at") as u64;
    }
    if aargs.flag_present("cookie") {
        conf.opt_cookie = aargs.get_string("cookie");
    }
    if aargs.flag_present("cookie-jar") {
        conf.opt_cookie_jar = aargs.get_string("cookie-jar");
    }
    if aargs.flag_present("create-dirs") {
        conf.flg_create_dirs = aargs.get_bool("create-dirs");
    }
    if aargs.flag_present("crlf") {
        conf.flg_crlf = aargs.get_bool("crlf");
    }
    if aargs.flag_present("crlfile") {
        conf.opt_crlfile = aargs.get_string("crlfile");
    }
    if aargs.flag_present("data") {
        conf.opt_data = aargs.get_string("data");
    }
    if aargs.flag_present("data-ascii") {
        conf.opt_data_ascii = aargs.get_string("data-ascii");
    }
    if aargs.flag_present("data-binary") {
        conf.opt_data_binary = aargs.get_string("data-binary");
    }
    if aargs.flag_present("data-raw") {
        conf.opt_data_raw = aargs.get_string("data-raw");
    }
    if aargs.flag_present("data-urlencode") {
        conf.opt_data_urlencode = aargs.get_string("data-urlencode");
    }
    if aargs.flag_present("delegation") {
        conf.opt_delegation = aargs.get_string("delegation");
    }
    if aargs.flag_present("digest") {
        conf.flg_digest = aargs.get_bool("digest");
    }
    if aargs.flag_present("disable") {
        conf.flg_disable = aargs.get_bool("disable");
    }
    if aargs.flag_present("disable-eprt") {
        conf.flg_disable_eprt = aargs.get_bool("disable-eprt");
    }
    if aargs.flag_present("disable-epsv") {
        conf.flg_disable_epsv = aargs.get_bool("disable-epsv");
    }
    if aargs.flag_present("disallow-username-in-url") {
        conf.flg_disallow_username_in_url = aargs.get_bool("disallow-username-in-url");
    }
    if aargs.flag_present("dns-interface") {
        conf.opt_dns_interface = aargs.get_string("dns-interface");
    }
    if aargs.flag_present("dns-ipv4-addr") {
        conf.opt_dns_ipv4_addr = aargs.get_string("dns-ipv4-addr");
    }
    if aargs.flag_present("dns-ipv6-addr") {
        conf.opt_dns_ipv6_addr = aargs.get_string("dns-ipv6-addr");
    }
    if aargs.flag_present("dns-servers") {
        conf.opt_dns_servers = aargs.get_string("dns-servers");
    }
    if aargs.flag_present("doh-url") {
        conf.opt_doh_url = aargs.get_string("doh-url");
    }
    if aargs.flag_present("dump-header") {
        conf.opt_dump_header = aargs.get_string("dump-header");
    }
    if aargs.flag_present("egd-file") {
        conf.opt_egd_file = aargs.get_string("egd-file");
    }
    if aargs.flag_present("engine") {
        conf.opt_engine = aargs.get_string("engine");
    }
    if aargs.flag_present("etag-save") {
        conf.opt_etag_save = aargs.get_string("etag-save");
    }
    if aargs.flag_present("etag-compare") {
        conf.opt_etag_compare = aargs.get_string("etag-compare");
    }
    if aargs.flag_present("expect100-timeout") {
        conf.opt_expect100_timeout = aargs.get_integer("expect100-timeout") as u32;
    }
    if aargs.flag_present("fail") {
        conf.flg_fail = aargs.get_bool("fail");
    }
    if aargs.flag_present("fail-early") {
        conf.flg_fail_early = aargs.get_bool("fail-early");
    }
    if aargs.flag_present("false-start") {
        conf.flg_false_start = aargs.get_bool("false-start");
    }
    if aargs.flag_present("form") {
        conf.opt_form = aargs.get_string("form");
    }
    if aargs.flag_present("form-string") {
        conf.opt_form_string = aargs.get_string("form-string");
    }
    if aargs.flag_present("ftp-account") {
        conf.opt_ftp_account = aargs.get_string("ftp-account");
    }
    if aargs.flag_present("ftp-alternative-to-user") {
        conf.opt_ftp_alternative_to_user = aargs.get_string("ftp-alternative-to-user");
    }
    if aargs.flag_present("ftp-create-dirs") {
        conf.flg_ftp_create_dirs = aargs.get_bool("ftp-create-dirs");
    }
    if aargs.flag_present("ftp-method") {
        conf.opt_ftp_method = aargs.get_string("ftp-method");
    }
    if aargs.flag_present("ftp-pasv") {
        conf.flg_ftp_pasv = aargs.get_bool("ftp-pasv");
    }
    if aargs.flag_present("ftp-port") {
        conf.opt_ftp_port = aargs.get_string("ftp-port");
    }
    if aargs.flag_present("ftp-pret") {
        conf.flg_ftp_pret = aargs.get_bool("ftp-pret");
    }
    if aargs.flag_present("ftp-skip-pasv-ip") {
        conf.flg_ftp_skip_pasv_ip = aargs.get_bool("ftp-skip-pasv-ip");
    }
    if aargs.flag_present("ftp-ssl-ccc") {
        conf.flg_ftp_ssl_ccc = aargs.get_bool("ftp-ssl-ccc");
    }
    if aargs.flag_present("ftp-ssl-ccc-mode") {
        conf.opt_ftp_ssl_ccc_mode = aargs.get_string("ftp-ssl-ccc-mode");
    }
    if aargs.flag_present("ftp-ssl-control") {
        conf.flg_ftp_ssl_control = aargs.get_bool("ftp-ssl-control");
    }
    if aargs.flag_present("get") {
        conf.flg_get = aargs.get_bool("get");
    }
    if aargs.flag_present("globoff") {
        conf.flg_globoff = aargs.get_bool("globoff");
    }
    if aargs.flag_present("happy-eyeballs-timeout-ms") {
        conf.opt_happy_eyeballs_timeout_ms = aargs.get_integer("happy-eyeballs-timeout-ms") as u64;
    }
    if aargs.flag_present("haproxy-protocol") {
        conf.flg_haproxy_protocol = aargs.get_bool("haproxy-protocol");
    }
    if aargs.flag_present("head") {
        conf.flg_head = aargs.get_bool("head");
    }
    if aargs.flag_present("header") {
        conf.opt_header = aargs.get_string("header");
    }
    if aargs.flag_present("hostpubmd5") {
        conf.opt_hostpubmd5 = aargs.get_string("hostpubmd5");
    }
    if aargs.flag_present("http0-9") {
        conf.flg_http0_9 = aargs.get_bool("http0-9");
    }
    if aargs.flag_present("http1-0") {
        conf.flg_http1_0 = aargs.get_bool("http1-0");
    }
    if aargs.flag_present("http1-1") {
        conf.flg_http1_1 = aargs.get_bool("http1-1");
    }
    if aargs.flag_present("http2") {
        conf.flg_http2 = aargs.get_bool("http2");
    }
    if aargs.flag_present("http2-prior-knowledge") {
        conf.flg_http2_prior_knowledge = aargs.get_bool("http2-prior-knowledge");
    }
    if aargs.flag_present("http3") {
        conf.flg_http3 = aargs.get_bool("http3");
    }
    if aargs.flag_present("ignore-content-length") {
        conf.flg_ignore_content_length = aargs.get_bool("ignore-content-length");
    }
    if aargs.flag_present("include") {
        conf.flg_include = aargs.get_bool("include");
    }
    if aargs.flag_present("insecure") {
        conf.flg_insecure = aargs.get_bool("insecure");
    }
    if aargs.flag_present("interface") {
        conf.opt_interface = aargs.get_string("interface");
    }
    if aargs.flag_present("ipv4") {
        conf.flg_ipv4 = aargs.get_bool("ipv4");
    }
    if aargs.flag_present("ipv6") {
        conf.flg_ipv6 = aargs.get_bool("ipv6");
    }
    if aargs.flag_present("junk-session-cookies") {
        conf.flg_junk_session_cookies = aargs.get_bool("junk-session-cookies");
    }
    if aargs.flag_present("keepalive-time") {
        conf.opt_keepalive_time = aargs.get_integer("keepalive-time") as u32;
    }
    if aargs.flag_present("key") {
        conf.opt_key = aargs.get_string("key");
    }
    if aargs.flag_present("key-type") {
        conf.opt_key_type = aargs.get_string("key-type");
    }
    if aargs.flag_present("krb") {
        conf.opt_krb = aargs.get_string("krb");
    }
    if aargs.flag_present("libcurl") {
        conf.opt_libcurl = aargs.get_string("libcurl");
    }
    if aargs.flag_present("limit-rate") {
        conf.opt_limit_rate = aargs.get_integer("limit-rate") as u64;
    }
    if aargs.flag_present("list-only") {
        conf.flg_list_only = aargs.get_bool("list-only");
    }
    if aargs.flag_present("local-port") {
        conf.opt_local_port = aargs.get_string("local-port");
    }
    if aargs.flag_present("location") {
        conf.flg_location = aargs.get_bool("location");
    }
    if aargs.flag_present("location-trusted") {
        conf.flg_location_trusted = aargs.get_bool("location-trusted");
    }
    if aargs.flag_present("login-options") {
        conf.opt_login_options = aargs.get_string("login-options");
    }
    if aargs.flag_present("mail-auth") {
        conf.opt_mail_auth = aargs.get_string("mail-auth");
    }
    if aargs.flag_present("mail-from") {
        conf.opt_mail_from = aargs.get_string("mail-from");
    }
    if aargs.flag_present("mail-rcpt") {
        conf.opt_mail_rcpt = aargs.get_string("mail-rcpt");
    }
    if aargs.flag_present("manual") {
        conf.flg_manual = aargs.get_bool("manual");
    }
    if aargs.flag_present("max-filesize") {
        conf.opt_max_filesize = aargs.get_integer("max-filesize") as u64;
    }
    if aargs.flag_present("max-redirs") {
        conf.opt_max_redirs = aargs.get_integer("max-redirs") as u32;
    }
    if aargs.flag_present("max-time") {
        conf.opt_max_time = aargs.get_integer("max-time") as u32;
    }
    if aargs.flag_present("metalink") {
        conf.flg_metalink = aargs.get_bool("metalink");
    }
    if aargs.flag_present("negotiate") {
        conf.flg_negotiate = aargs.get_bool("negotiate");
    }
    if aargs.flag_present("netrc") {
        conf.flg_netrc = aargs.get_bool("netrc");
    }
    if aargs.flag_present("netrc-file") {
        conf.opt_netrc_file = aargs.get_string("netrc-file");
    }
    if aargs.flag_present("netrc-optional") {
        conf.flg_netrc_optional = aargs.get_bool("netrc-optional");
    }
    if aargs.flag_present("next") {
        conf.flg_next = aargs.get_bool("next");
    }
    if aargs.flag_present("no-alpn") {
        conf.flg_no_alpn = aargs.get_bool("no-alpn");
    }
    if aargs.flag_present("no-buffer") {
        conf.flg_no_buffer = aargs.get_bool("no-buffer");
    }
    if aargs.flag_present("no-keepalive") {
        conf.flg_no_keepalive = aargs.get_bool("no-keepalive");
    }
    if aargs.flag_present("no-npn") {
        conf.flg_no_npn = aargs.get_bool("no-npn");
    }
    if aargs.flag_present("no-progress-meter") {
        conf.flg_no_progress_meter = aargs.get_bool("no-progress-meter");
    }
    if aargs.flag_present("no-sessionid") {
        conf.flg_no_sessionid = aargs.get_bool("no-sessionid");
    }
    if aargs.flag_present("noproxy") {
        conf.opt_noproxy = aargs.get_string("noproxy");
    }
    if aargs.flag_present("ntlm") {
        conf.flg_ntlm = aargs.get_bool("ntlm");
    }
    if aargs.flag_present("ntlm-wb") {
        conf.flg_ntlm_wb = aargs.get_bool("ntlm-wb");
    }
    if aargs.flag_present("oauth2-bearer") {
        conf.opt_oauth2_bearer = aargs.get_string("oauth2-bearer");
    }
    if aargs.flag_present("output") {
        conf.opt_output = aargs.get_string("output");
    }
    if aargs.flag_present("parallel") {
        conf.flg_parallel = aargs.get_bool("parallel");
    }
    if aargs.flag_present("parallel-immediate") {
        conf.flg_parallel_immediate = aargs.get_bool("parallel-immediate");
    }
    if aargs.flag_present("parallel-max") {
        conf.flg_parallel_max = aargs.get_bool("parallel-max");
    }
    if aargs.flag_present("pass") {
        conf.opt_pass = aargs.get_string("pass");
    }
    if aargs.flag_present("path-as-is") {
        conf.flg_path_as_is = aargs.get_bool("path-as-is");
    }
    if aargs.flag_present("pinnedpubkey") {
        conf.opt_pinnedpubkey = aargs.get_string("pinnedpubkey");
    }
    if aargs.flag_present("post301") {
        conf.flg_post301 = aargs.get_bool("post301");
    }
    if aargs.flag_present("post302") {
        conf.flg_post302 = aargs.get_bool("post302");
    }
    if aargs.flag_present("post303") {
        conf.flg_post303 = aargs.get_bool("post303");
    }
    if aargs.flag_present("preproxy") {
        conf.opt_preproxy = aargs.get_string("preproxy");
    }
    if aargs.flag_present("progress-bar") {
        conf.flg_progress_bar = aargs.get_bool("progress-bar");
    }
    if aargs.flag_present("proto") {
        conf.opt_proto = aargs.get_string("proto");
    }
    if aargs.flag_present("proto-default") {
        conf.opt_proto_default = aargs.get_string("proto-default");
    }
    if aargs.flag_present("proto-redir") {
        conf.opt_proto_redir = aargs.get_string("proto-redir");
    }
    if aargs.flag_present("proxy") {
        conf.opt_proxy = aargs.get_string("proxy");
    }
    if aargs.flag_present("proxy-anyauth") {
        conf.flg_proxy_anyauth = aargs.get_bool("proxy-anyauth");
    }
    if aargs.flag_present("proxy-basic") {
        conf.flg_proxy_basic = aargs.get_bool("proxy-basic");
    }
    if aargs.flag_present("proxy-cacert") {
        conf.opt_proxy_cacert = aargs.get_string("proxy-cacert");
    }
    if aargs.flag_present("proxy-capath") {
        conf.opt_proxy_capath = aargs.get_string("proxy-capath");
    }
    if aargs.flag_present("proxy-cert") {
        conf.opt_proxy_cert = aargs.get_string("proxy-cert");
    }
    if aargs.flag_present("proxy-cert-type") {
        conf.opt_proxy_cert_type = aargs.get_string("proxy-cert-type");
    }
    if aargs.flag_present("proxy-ciphers") {
        conf.opt_proxy_ciphers = aargs.get_string("proxy-ciphers");
    }
    if aargs.flag_present("proxy-crlfile") {
        conf.opt_proxy_crlfile = aargs.get_string("proxy-crlfile");
    }
    if aargs.flag_present("proxy-digest") {
        conf.flg_proxy_digest = aargs.get_bool("proxy-digest");
    }
    if aargs.flag_present("proxy-header") {
        conf.opt_proxy_header = aargs.get_string("proxy-header");
    }
    if aargs.flag_present("proxy-insecure") {
        conf.flg_proxy_insecure = aargs.get_bool("proxy-insecure");
    }
    if aargs.flag_present("proxy-key") {
        conf.opt_proxy_key = aargs.get_string("proxy-key");
    }
    if aargs.flag_present("proxy-key-type") {
        conf.opt_proxy_key_type = aargs.get_string("proxy-key-type");
    }
    if aargs.flag_present("proxy-negotiate") {
        conf.flg_proxy_negotiate = aargs.get_bool("proxy-negotiate");
    }
    if aargs.flag_present("proxy-ntlm") {
        conf.flg_proxy_ntlm = aargs.get_bool("proxy-ntlm");
    }
    if aargs.flag_present("proxy-pass") {
        conf.opt_proxy_pass = aargs.get_string("proxy-pass");
    }
    if aargs.flag_present("proxy-pinnedpubkey") {
        conf.opt_proxy_pinnedpubkey = aargs.get_string("proxy-pinnedpubkey");
    }
    if aargs.flag_present("proxy-service-name") {
        conf.opt_proxy_service_name = aargs.get_string("proxy-service-name");
    }
    if aargs.flag_present("proxy-ssl-allow-beast") {
        conf.flg_proxy_ssl_allow_beast = aargs.get_bool("proxy-ssl-allow-beast");
    }
    if aargs.flag_present("proxy-tls13-ciphers") {
        conf.opt_proxy_tls13_ciphers = aargs.get_string("proxy-tls13-ciphers");
    }
    if aargs.flag_present("proxy-tlsauthtype") {
        conf.opt_proxy_tlsauthtype = aargs.get_string("proxy-tlsauthtype");
    }
    if aargs.flag_present("proxy-tlspassword") {
        conf.opt_proxy_tlspassword = aargs.get_string("proxy-tlspassword");
    }
    if aargs.flag_present("proxy-tlsuser") {
        conf.opt_proxy_tlsuser = aargs.get_string("proxy-tlsuser");
    }
    if aargs.flag_present("proxy-tlsv1") {
        conf.flg_proxy_tlsv1 = aargs.get_bool("proxy-tlsv1");
    }
    if aargs.flag_present("proxy-user") {
        conf.opt_proxy_user = aargs.get_string("proxy-user");
    }
    if aargs.flag_present("proxy1-0") {
        conf.opt_proxy1_0 = aargs.get_string("proxy1-0");
    }
    if aargs.flag_present("proxytunnel") {
        conf.flg_proxytunnel = aargs.get_bool("proxytunnel");
    }
    if aargs.flag_present("pubkey") {
        conf.opt_pubkey = aargs.get_string("pubkey");
    }
    if aargs.flag_present("quote") {
        conf.flg_quote = aargs.get_bool("quote");
    }
    if aargs.flag_present("random-file") {
        conf.opt_random_file = aargs.get_string("random-file");
    }
    if aargs.flag_present("range") {
        conf.opt_range = aargs.get_string("range");
    }
    if aargs.flag_present("raw") {
        conf.flg_raw = aargs.get_bool("raw");
    }
    if aargs.flag_present("referer") {
        conf.opt_referer = aargs.get_string("referer");
    }
    if aargs.flag_present("remote-header-name") {
        conf.flg_remote_header_name = aargs.get_bool("remote-header-name");
    }
    if aargs.flag_present("remote-name") {
        conf.flg_remote_name = aargs.get_bool("remote-name");
    }
    if aargs.flag_present("remote-name-all") {
        conf.flg_remote_name_all = aargs.get_bool("remote-name-all");
    }
    if aargs.flag_present("remote-time") {
        conf.flg_remote_time = aargs.get_bool("remote-time");
    }
    if aargs.flag_present("request") {
        conf.opt_request = aargs.get_string("request");
    }
    if aargs.flag_present("request-target") {
        conf.flg_request_target = aargs.get_bool("request-target");
    }
    if aargs.flag_present("resolve") {
        conf.opt_resolve = aargs.get_string("resolve");
    }
    if aargs.flag_present("retry") {
        conf.opt_retry = aargs.get_integer("retry") as u32;
    }
    if aargs.flag_present("retry-connrefused") {
        conf.flg_retry_connrefused = aargs.get_bool("retry-connrefused");
    }
    if aargs.flag_present("retry-delay") {
        conf.opt_retry_delay = aargs.get_integer("retry-delay") as u32;
    }
    if aargs.flag_present("retry-max-time") {
        conf.opt_retry_max_time = aargs.get_integer("retry-max-time") as u32;
    }
    if aargs.flag_present("sasl-authzid") {
        conf.opt_sasl_authzid = aargs.get_string("sasl-authzid");
    }
    if aargs.flag_present("sasl-ir") {
        conf.flg_sasl_ir = aargs.get_bool("sasl-ir");
    }
    if aargs.flag_present("service-name") {
        conf.opt_service_name = aargs.get_string("service-name");
    }
    if aargs.flag_present("show-error") {
        conf.flg_show_error = aargs.get_bool("show-error");
    }
    if aargs.flag_present("silent") {
        conf.flg_silent = aargs.get_bool("silent");
    }
    if aargs.flag_present("socks4") {
        conf.opt_socks4 = aargs.get_string("socks4");
    }
    if aargs.flag_present("socks4a") {
        conf.opt_socks4a = aargs.get_string("socks4a");
    }
    if aargs.flag_present("socks5") {
        conf.opt_socks5 = aargs.get_string("socks5");
    }
    if aargs.flag_present("socks5-basic") {
        conf.flg_socks5_basic = aargs.get_bool("socks5-basic");
    }
    if aargs.flag_present("socks5-gssapi") {
        conf.flg_socks5_gssapi = aargs.get_bool("socks5-gssapi");
    }
    if aargs.flag_present("socks5-gssapi-nec") {
        conf.flg_socks5_gssapi_nec = aargs.get_bool("socks5-gssapi-nec");
    }
    if aargs.flag_present("socks5-gssapi-service") {
        conf.opt_socks5_gssapi_service = aargs.get_string("socks5-gssapi-service");
    }
    if aargs.flag_present("socks5-hostname") {
        conf.opt_socks5_hostname = aargs.get_string("socks5-hostname");
    }
    if aargs.flag_present("speed-limit") {
        conf.opt_speed_limit = aargs.get_integer("speed-limit") as u64;
    }
    if aargs.flag_present("speed-time") {
        conf.opt_speed_time = aargs.get_integer("speed-time") as u32;
    }
    if aargs.flag_present("ssl") {
        conf.flg_ssl = aargs.get_bool("ssl");
    }
    if aargs.flag_present("ssl-allow-beast") {
        conf.flg_ssl_allow_beast = aargs.get_bool("ssl-allow-beast");
    }
    if aargs.flag_present("ssl-no-revoke") {
        conf.flg_ssl_no_revoke = aargs.get_bool("ssl-no-revoke");
    }
    if aargs.flag_present("ssl-reqd") {
        conf.flg_ssl_reqd = aargs.get_bool("ssl-reqd");
    }
    if aargs.flag_present("sslv2") {
        conf.flg_sslv2 = aargs.get_bool("sslv2");
    }
    if aargs.flag_present("sslv3") {
        conf.flg_sslv3 = aargs.get_bool("sslv3");
    }
    if aargs.flag_present("stderr") {
        conf.flg_stderr = aargs.get_bool("stderr");
    }
    if aargs.flag_present("styled-output") {
        conf.flg_styled_output = aargs.get_bool("styled-output");
    }
    if aargs.flag_present("suppress-connect-headers") {
        conf.flg_suppress_connect_headers = aargs.get_bool("suppress-connect-headers");
    }
    if aargs.flag_present("tcp-fastopen") {
        conf.flg_tcp_fastopen = aargs.get_bool("tcp-fastopen");
    }
    if aargs.flag_present("tcp-nodelay") {
        conf.flg_tcp_nodelay = aargs.get_bool("tcp-nodelay");
    }
    if aargs.flag_present("telnet-option") {
        conf.opt_telnet_option = aargs.get_string("telnet-option");
    }
    if aargs.flag_present("tftp-blksize") {
        conf.opt_tftp_blksize = aargs.get_integer("tftp-blksize") as u32;
    }
    if aargs.flag_present("tftp-no-options") {
        conf.flg_tftp_no_options = aargs.get_bool("tftp-no-options");
    }
    if aargs.flag_present("time-cond") {
        conf.opt_time_cond = aargs.get_string("time-cond");
    }
    if aargs.flag_present("tls-max") {
        conf.opt_tls_max = aargs.get_string("tls-max");
    }
    if aargs.flag_present("tls13-ciphers") {
        conf.opt_tls13_ciphers = aargs.get_string("tls13-ciphers");
    }
    if aargs.flag_present("tlsauthtype") {
        conf.opt_tlsauthtype = aargs.get_string("tlsauthtype");
    }
    if aargs.flag_present("tlspassword") {
        conf.flg_tlspassword = aargs.get_bool("tlspassword");
    }
    if aargs.flag_present("tlsuser") {
        conf.opt_tlsuser = aargs.get_string("tlsuser");
    }
    if aargs.flag_present("tlsv1") {
        conf.flg_tlsv1 = aargs.get_bool("tlsv1");
    }
    if aargs.flag_present("tlsv1-0") {
        conf.flg_tlsv1_0 = aargs.get_bool("tlsv1-0");
    }
    if aargs.flag_present("tlsv1-1") {
        conf.flg_tlsv1_1 = aargs.get_bool("tlsv1-1");
    }
    if aargs.flag_present("tlsv1-2") {
        conf.flg_tlsv1_2 = aargs.get_bool("tlsv1-2");
    }
    if aargs.flag_present("tlsv1-3") {
        conf.flg_tlsv1_3 = aargs.get_bool("tlsv1-3");
    }
    if aargs.flag_present("tr-encoding") {
        conf.flg_tr_encoding = aargs.get_bool("tr-encoding");
    }
    if aargs.flag_present("trace") {
        conf.opt_trace = aargs.get_string("trace");
    }
    if aargs.flag_present("trace-ascii") {
        conf.opt_trace_ascii = aargs.get_string("trace-ascii");
    }
    if aargs.flag_present("trace-time") {
        conf.flg_trace_time = aargs.get_bool("trace-time");
    }
    if aargs.flag_present("unix-socket") {
        conf.opt_unix_socket = aargs.get_string("unix-socket");
    }
    if aargs.flag_present("upload-file") {
        conf.opt_upload_file = aargs.get_string("upload-file");
    }
    if aargs.flag_present("url") {
        conf.opt_url = aargs.get_string("url");
    }
    if aargs.flag_present("use-ascii") {
        conf.flg_use_ascii = aargs.get_bool("use-ascii");
    }
    if aargs.flag_present("user") {
        conf.opt_user = aargs.get_string("user");
    }
    if aargs.flag_present("user-agent") {
        conf.opt_user_agent = aargs.get_string("user-agent");
    }
    if aargs.flag_present("verbose") {
        conf.flg_verbose = aargs.get_bool("verbose");
    }
    if aargs.flag_present("write-out") {
        conf.opt_write_out = aargs.get_string("write-out");
    }
    if aargs.flag_present("xattr") {
        conf.flg_xattr = aargs.get_bool("xattr");
    }

    if let Ok(b) = aargs.get_bool_result("help") {
        if b {
            print_help_and_exit(conf);
        }
    }

    if let Ok(b) = aargs.get_bool_result("version") {
        if b {
            print_version_and_exit(conf);
        }
    }

}
